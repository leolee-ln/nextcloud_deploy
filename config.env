# Deployment configuration
# Base directory for all container data
DATA_DIR=/data_raid1/containers

# Nextcloud external port on the host. Default: 8443
NEXTCLOUD_PORT=8443

# Nextcloud domain (used for trusted domains and overwrite URLs)
NEXTCLOUD_DOMAIN=ic.ismd-nemo.xyz

# MySQL external port on the host. Leave empty to NOT expose MySQL externally.
# Example to expose on host port 3306: MYSQL_EXPOSE_PORT=3306
MYSQL_EXPOSE_PORT=8306

# End of file
